/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.1
 **/

#ifndef __ecat_debug_data_lcmt_hpp__
#define __ecat_debug_data_lcmt_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>


class ecat_debug_data_lcmt
{
    public:
        int32_t    dim;

        /**
         * LCM Type: int16_t[dim]
         */
        std::vector< int16_t > stat;

        /**
         * LCM Type: int16_t[dim]
         */
        std::vector< int16_t > res1;

        /**
         * LCM Type: int16_t[dim]
         */
        std::vector< int16_t > res2;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > mc_curPos;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > mc_curVel;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > mc_curCur;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > mc_curTor;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > original_curPosH;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > original_curPosL;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > original_curVel;

        /**
         * LCM Type: float[dim]
         */
        std::vector< float > original_curTor;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "ecat_debug_data_lcmt"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int ecat_debug_data_lcmt::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int ecat_debug_data_lcmt::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int ecat_debug_data_lcmt::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t ecat_debug_data_lcmt::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* ecat_debug_data_lcmt::getTypeName()
{
    return "ecat_debug_data_lcmt";
}

int ecat_debug_data_lcmt::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->dim, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->dim > 0) {
        tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->stat[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->res1[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->res2[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->mc_curPos[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->mc_curVel[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->mc_curCur[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->mc_curTor[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->original_curPosH[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->original_curPosL[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->original_curVel[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim > 0) {
        tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->original_curTor[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int ecat_debug_data_lcmt::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->dim, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->dim) {
        this->stat.resize(this->dim);
        tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->stat[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->res1.resize(this->dim);
        tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->res1[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->res2.resize(this->dim);
        tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->res2[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->mc_curPos.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->mc_curPos[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->mc_curVel.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->mc_curVel[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->mc_curCur.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->mc_curCur[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->mc_curTor.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->mc_curTor[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->original_curPosH.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->original_curPosH[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->original_curPosL.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->original_curPosL[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->original_curVel.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->original_curVel[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->dim) {
        this->original_curTor.resize(this->dim);
        tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->original_curTor[0], this->dim);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int ecat_debug_data_lcmt::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, this->dim);
    enc_size += __int16_t_encoded_array_size(NULL, this->dim);
    enc_size += __int16_t_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    enc_size += __float_encoded_array_size(NULL, this->dim);
    return enc_size;
}

uint64_t ecat_debug_data_lcmt::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0x4f6abd0226bb50f6LL;
    return (hash<<1) + ((hash>>63)&1);
}

#endif
