/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 * lcm-gen 1.5.1
 */

package lcmtypes;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class floating_base_cmd_lcmt implements lcm.lcm.LCMEncodable
{
    /**
     * LCM Type: float[3]
     */
    public float delta_pos_cmd[];

    /**
     * dx dy dz
     * LCM Type: float[3]
     */
    public float abs_ori_cmd[];

 
    public floating_base_cmd_lcmt()
    {
        delta_pos_cmd = new float[3];
        abs_ori_cmd = new float[3];
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x092bcaeab760a9b3L;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(lcmtypes.floating_base_cmd_lcmt.class))
            return 0L;
 
        classes.add(lcmtypes.floating_base_cmd_lcmt.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.delta_pos_cmd[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.abs_ori_cmd[a]); 
        }
 
    }
 
    public floating_base_cmd_lcmt(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public floating_base_cmd_lcmt(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static lcmtypes.floating_base_cmd_lcmt _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        lcmtypes.floating_base_cmd_lcmt o = new lcmtypes.floating_base_cmd_lcmt();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.delta_pos_cmd = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.delta_pos_cmd[a] = ins.readFloat();
        }
 
        this.abs_ori_cmd = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.abs_ori_cmd[a] = ins.readFloat();
        }
 
    }
 
    public lcmtypes.floating_base_cmd_lcmt copy()
    {
        lcmtypes.floating_base_cmd_lcmt outobj = new lcmtypes.floating_base_cmd_lcmt();
        outobj.delta_pos_cmd = new float[(int) 3];
        System.arraycopy(this.delta_pos_cmd, 0, outobj.delta_pos_cmd, 0, 3); 
        outobj.abs_ori_cmd = new float[(int) 3];
        System.arraycopy(this.abs_ori_cmd, 0, outobj.abs_ori_cmd, 0, 3); 
        return outobj;
    }
 
}

