/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 * lcm-gen 1.5.1
 */

package lcmtypes;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class walk_data_lcmt implements lcm.lcm.LCMEncodable
{
    /**
     * LCM Type: float[3]
     */
    public float refMPCLinPos_W[];

    /**
     * LCM Type: float[3]
     */
    public float refMPCLinVel_W[];

    /**
     * LCM Type: float[3]
     */
    public float refRobotLinPos_W[];

    /**
     * LCM Type: float[3]
     */
    public float desRobotLinPos_W[];

    /**
     * LCM Type: float[3]
     */
    public float desMPCLinVel_W[];

    /**
     * LCM Type: float[6]
     */
    public float swingLegCompensationByV_W[];

    /**
     * LCM Type: float[2]
     */
    public float pfx_rel[];

    /**
     * LCM Type: float[2]
     */
    public float pfy_rel[];

    /**
     * LCM Type: float[2]
     */
    public float lp_vx[];

    /**
     * LCM Type: float[2]
     */
    public float lp_vy[];

    /**
     * LCM Type: float[2]
     */
    public float isEarlyContact[];

    /**
     * LCM Type: float[2]
     */
    public float isDelayContact[];

 
    public walk_data_lcmt()
    {
        refMPCLinPos_W = new float[3];
        refMPCLinVel_W = new float[3];
        refRobotLinPos_W = new float[3];
        desRobotLinPos_W = new float[3];
        desMPCLinVel_W = new float[3];
        swingLegCompensationByV_W = new float[6];
        pfx_rel = new float[2];
        pfy_rel = new float[2];
        lp_vx = new float[2];
        lp_vy = new float[2];
        isEarlyContact = new float[2];
        isDelayContact = new float[2];
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x2838a2ef92dc66c8L;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(lcmtypes.walk_data_lcmt.class))
            return 0L;
 
        classes.add(lcmtypes.walk_data_lcmt.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.refMPCLinPos_W[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.refMPCLinVel_W[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.refRobotLinPos_W[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.desRobotLinPos_W[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.desMPCLinVel_W[a]); 
        }
 
        for (int a = 0; a < 6; a++) {
            outs.writeFloat(this.swingLegCompensationByV_W[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            outs.writeFloat(this.pfx_rel[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            outs.writeFloat(this.pfy_rel[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            outs.writeFloat(this.lp_vx[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            outs.writeFloat(this.lp_vy[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            outs.writeFloat(this.isEarlyContact[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            outs.writeFloat(this.isDelayContact[a]); 
        }
 
    }
 
    public walk_data_lcmt(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public walk_data_lcmt(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static lcmtypes.walk_data_lcmt _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        lcmtypes.walk_data_lcmt o = new lcmtypes.walk_data_lcmt();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.refMPCLinPos_W = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.refMPCLinPos_W[a] = ins.readFloat();
        }
 
        this.refMPCLinVel_W = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.refMPCLinVel_W[a] = ins.readFloat();
        }
 
        this.refRobotLinPos_W = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.refRobotLinPos_W[a] = ins.readFloat();
        }
 
        this.desRobotLinPos_W = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.desRobotLinPos_W[a] = ins.readFloat();
        }
 
        this.desMPCLinVel_W = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.desMPCLinVel_W[a] = ins.readFloat();
        }
 
        this.swingLegCompensationByV_W = new float[(int) 6];
        for (int a = 0; a < 6; a++) {
            this.swingLegCompensationByV_W[a] = ins.readFloat();
        }
 
        this.pfx_rel = new float[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.pfx_rel[a] = ins.readFloat();
        }
 
        this.pfy_rel = new float[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.pfy_rel[a] = ins.readFloat();
        }
 
        this.lp_vx = new float[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.lp_vx[a] = ins.readFloat();
        }
 
        this.lp_vy = new float[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.lp_vy[a] = ins.readFloat();
        }
 
        this.isEarlyContact = new float[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.isEarlyContact[a] = ins.readFloat();
        }
 
        this.isDelayContact = new float[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.isDelayContact[a] = ins.readFloat();
        }
 
    }
 
    public lcmtypes.walk_data_lcmt copy()
    {
        lcmtypes.walk_data_lcmt outobj = new lcmtypes.walk_data_lcmt();
        outobj.refMPCLinPos_W = new float[(int) 3];
        System.arraycopy(this.refMPCLinPos_W, 0, outobj.refMPCLinPos_W, 0, 3); 
        outobj.refMPCLinVel_W = new float[(int) 3];
        System.arraycopy(this.refMPCLinVel_W, 0, outobj.refMPCLinVel_W, 0, 3); 
        outobj.refRobotLinPos_W = new float[(int) 3];
        System.arraycopy(this.refRobotLinPos_W, 0, outobj.refRobotLinPos_W, 0, 3); 
        outobj.desRobotLinPos_W = new float[(int) 3];
        System.arraycopy(this.desRobotLinPos_W, 0, outobj.desRobotLinPos_W, 0, 3); 
        outobj.desMPCLinVel_W = new float[(int) 3];
        System.arraycopy(this.desMPCLinVel_W, 0, outobj.desMPCLinVel_W, 0, 3); 
        outobj.swingLegCompensationByV_W = new float[(int) 6];
        System.arraycopy(this.swingLegCompensationByV_W, 0, outobj.swingLegCompensationByV_W, 0, 6); 
        outobj.pfx_rel = new float[(int) 2];
        System.arraycopy(this.pfx_rel, 0, outobj.pfx_rel, 0, 2); 
        outobj.pfy_rel = new float[(int) 2];
        System.arraycopy(this.pfy_rel, 0, outobj.pfy_rel, 0, 2); 
        outobj.lp_vx = new float[(int) 2];
        System.arraycopy(this.lp_vx, 0, outobj.lp_vx, 0, 2); 
        outobj.lp_vy = new float[(int) 2];
        System.arraycopy(this.lp_vy, 0, outobj.lp_vy, 0, 2); 
        outobj.isEarlyContact = new float[(int) 2];
        System.arraycopy(this.isEarlyContact, 0, outobj.isEarlyContact, 0, 2); 
        outobj.isDelayContact = new float[(int) 2];
        System.arraycopy(this.isDelayContact, 0, outobj.isDelayContact, 0, 2); 
        return outobj;
    }
 
}

