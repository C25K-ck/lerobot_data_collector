"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class ecat_debug_cmd_lcmt(object):

    __slots__ = ["dim", "ctrlWord", "mc_kp", "mc_kd", "mc_tarPos", "mc_tarVel", "mc_tarTor", "original_tarPos", "original_tarVel", "original_tarTor"]

    __typenames__ = ["int32_t", "int16_t", "float", "float", "float", "float", "float", "float", "float", "float"]

    __dimensions__ = [None, ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"]]

    def __init__(self):
        self.dim = 0
        """ LCM Type: int32_t """
        self.ctrlWord = []
        """ LCM Type: int16_t[dim] """
        self.mc_kp = []
        """ LCM Type: float[dim] """
        self.mc_kd = []
        """ LCM Type: float[dim] """
        self.mc_tarPos = []
        """ LCM Type: float[dim] """
        self.mc_tarVel = []
        """ LCM Type: float[dim] """
        self.mc_tarTor = []
        """ LCM Type: float[dim] """
        self.original_tarPos = []
        """ LCM Type: float[dim] """
        self.original_tarVel = []
        """ LCM Type: float[dim] """
        self.original_tarTor = []
        """ LCM Type: float[dim] """

    def encode(self):
        buf = BytesIO()
        buf.write(ecat_debug_cmd_lcmt._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.dim))
        buf.write(struct.pack('>%dh' % self.dim, *self.ctrlWord[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_kp[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_kd[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_tarPos[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_tarVel[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_tarTor[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_tarPos[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_tarVel[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_tarTor[:self.dim]))

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ecat_debug_cmd_lcmt._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ecat_debug_cmd_lcmt._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = ecat_debug_cmd_lcmt()
        self.dim = struct.unpack(">i", buf.read(4))[0]
        self.ctrlWord = struct.unpack('>%dh' % self.dim, buf.read(self.dim * 2))
        self.mc_kp = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_kd = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_tarPos = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_tarVel = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_tarTor = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_tarPos = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_tarVel = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_tarTor = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if ecat_debug_cmd_lcmt in parents: return 0
        tmphash = (0x19ad9f353d6fd46c) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if ecat_debug_cmd_lcmt._packed_fingerprint is None:
            ecat_debug_cmd_lcmt._packed_fingerprint = struct.pack(">Q", ecat_debug_cmd_lcmt._get_hash_recursive([]))
        return ecat_debug_cmd_lcmt._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", ecat_debug_cmd_lcmt._get_packed_fingerprint())[0]

