"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class ecat_debug_data_lcmt(object):

    __slots__ = ["dim", "stat", "res1", "res2", "mc_curPos", "mc_curVel", "mc_curCur", "mc_curTor", "original_curPosH", "original_curPosL", "original_curVel", "original_curTor"]

    __typenames__ = ["int32_t", "int16_t", "int16_t", "int16_t", "float", "float", "float", "float", "float", "float", "float", "float"]

    __dimensions__ = [None, ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"], ["dim"]]

    def __init__(self):
        self.dim = 0
        """ LCM Type: int32_t """
        self.stat = []
        """ LCM Type: int16_t[dim] """
        self.res1 = []
        """ LCM Type: int16_t[dim] """
        self.res2 = []
        """ LCM Type: int16_t[dim] """
        self.mc_curPos = []
        """ LCM Type: float[dim] """
        self.mc_curVel = []
        """ LCM Type: float[dim] """
        self.mc_curCur = []
        """ LCM Type: float[dim] """
        self.mc_curTor = []
        """ LCM Type: float[dim] """
        self.original_curPosH = []
        """ LCM Type: float[dim] """
        self.original_curPosL = []
        """ LCM Type: float[dim] """
        self.original_curVel = []
        """ LCM Type: float[dim] """
        self.original_curTor = []
        """ LCM Type: float[dim] """

    def encode(self):
        buf = BytesIO()
        buf.write(ecat_debug_data_lcmt._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.dim))
        buf.write(struct.pack('>%dh' % self.dim, *self.stat[:self.dim]))
        buf.write(struct.pack('>%dh' % self.dim, *self.res1[:self.dim]))
        buf.write(struct.pack('>%dh' % self.dim, *self.res2[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_curPos[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_curVel[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_curCur[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.mc_curTor[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_curPosH[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_curPosL[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_curVel[:self.dim]))
        buf.write(struct.pack('>%df' % self.dim, *self.original_curTor[:self.dim]))

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ecat_debug_data_lcmt._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ecat_debug_data_lcmt._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = ecat_debug_data_lcmt()
        self.dim = struct.unpack(">i", buf.read(4))[0]
        self.stat = struct.unpack('>%dh' % self.dim, buf.read(self.dim * 2))
        self.res1 = struct.unpack('>%dh' % self.dim, buf.read(self.dim * 2))
        self.res2 = struct.unpack('>%dh' % self.dim, buf.read(self.dim * 2))
        self.mc_curPos = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_curVel = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_curCur = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.mc_curTor = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_curPosH = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_curPosL = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_curVel = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        self.original_curTor = struct.unpack('>%df' % self.dim, buf.read(self.dim * 4))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if ecat_debug_data_lcmt in parents: return 0
        tmphash = (0x4f6abd0226bb50f6) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if ecat_debug_data_lcmt._packed_fingerprint is None:
            ecat_debug_data_lcmt._packed_fingerprint = struct.pack(">Q", ecat_debug_data_lcmt._get_hash_recursive([]))
        return ecat_debug_data_lcmt._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", ecat_debug_data_lcmt._get_packed_fingerprint())[0]

