# LeRobot 数据采集平台 — 完整 Python 依赖清单
# 由 install_all_deps.sh 自动安装；也可手动: pip install -r requirements-full.txt

# ========== 核心（requirements.txt）==========
numpy>=1.20.0
PySide6>=6.5.0
pandas>=1.3.0
h5py>=3.0.0
pyarrow>=5.0.0
fastparquet>=0.7.0
requests>=2.28.0
minio>=7.2.0

# ========== LeRobot 数据集写入（lerobot_factory 运行时必需）==========
torch>=2.2.1
torchvision>=0.21.0
Pillow>=10.0.0
datasets>=2.19.0
huggingface-hub>=0.27.1
packaging>=24.2
jsonlines>=4.0.0
imageio[ffmpeg]>=2.34.0
av>=12.0.5,<13.0.0

# ========== GUI 回退 ==========
PyQt6>=6.5.0

# ========== 相机 ==========
opencv-python>=4.9.0
pyrealsense2

# ========== 奥比中光（需系统 Orbbec SDK，见 install_all_deps.sh）==========
pyorbbecsdk

# ========== Gen1 LCM / Pico 遥操作 / robot_kinemic ==========
lcm
grpcio
grpcio-tools
protobuf
scipy
pin

# ========== robot_kinemic 调试与辅助 ==========
PyQt5
matplotlib
pynput
psutil

# ========== 可选：进度条 / CLI（lerobot_unit 可选导入）==========
tqdm
tyro

# ========== 开发 / 测试 ==========
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0

# ========== 可选：GPU 深度缩放（convert_to_target_format.py，需 CUDA）==========
# cupy 由 install_all_deps.sh 按 CUDA 版本单独尝试安装
